var config = {}

// Elasticsearch Source
config.elUrl = 'http://192.168.41.19:9200'
config.index = 'tommy'
config.index = 'test'
config.postContent = {
    "query": {
        "bool": {
            "must": [{
                "match_all": {}
            }],
            "must_not": [],
            "should": []
        }
    },
    "from": 0,
    "size": 100,
    "sort": [],
    "aggs": {}
}

// csv file name
config.filename = 'el.csv'

module.exports = config
